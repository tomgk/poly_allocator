
| No. | Category | Short Description | Location (File:Line) | Impact |
| :--- | :--- | :--- | :--- | :--- |
| **1** | Critical Bug (UB) | `allocateArray` incorrectly registers the single-object destructor `callback::DestructObject` instead of `Callback_DestructArray`. | `arena_allocator.hpp:449` | **Crash / Memory Leak** for arrays of complex types (destroys only the 1st element). |
| **2** | Critical Bug (UB) | `allocateArray` (with primitives) confuses the `bool zero_initialize` flag with the `value` parameter of `allocateArrayWithDefault`. | `arena_allocator.hpp:544` | **Random Test Fails**: Interprets `true` (value 1) as an object to be copied instead of zeroing the memory. |
| **3** | Critical Bug (UB) | During reallocation (`reallocate`), complex objects are moved via `new_header.copy(...)`, but old instances are wiped out via internal `destroy_objects_in_range` destructors afterwards. | `arena_allocator.hpp:202` | **Double Free / Crash** for heap-allocating types (like `std::string` or `Data`) due to destructors running on invalid addresses. |
| **4** | Logic Error / UB | `compact()` shifts alive objects forward, but then mistakenly calls the destructor on the *new* destination if `aligned_write_offset < read_offset`. | `arena_allocator.hpp:276` | **Instant Data Destruction**: Destroys the freshly compacted object in memory. |
| **5** | Architecture / UB | `get_header` and `get_object_pointer` use unchecked `reinterpret_cast` on `vector.data()`, causing pointer invalidation during reallocations. | `arena_allocator.hpp:33` | **Dangling Pointer**: `ArenaBufferHandle` or iterators crash if the arena capacity increases in the background. |
| **6** | Logic Error | `get_dead_bytes()` calculates alignment for the next object statically via `alignof(AllocationHeader)` instead of the actual alignment of the stored type. | `arena_allocator.hpp:144` | **Infinite Loop / Wrong Metrics**: The tracking loop loses synchronization with the actual header layout in the raw byte buffer. |
| **7** | Logic Error | `get_allocation_count()` counts the total number of *elements* within an array instead of the number of unique *allocation blocks*. | `arena_allocator.hpp:513` | **Incorrect Metrics**: An array with 10 `ints` is counted as 10 active allocations, even though it only owns a single header. |
| **8** | Performance | `allocate0` invokes `buffer.resize(end_offset)`, which zero-initializes unused memory by default. | `arena_allocator.hpp:482` | **Double Writing**: Wastes CPU cycles by redundantly zeroing out raw memory that gets overwritten by the object constructor immediately after. |
| **9** | API Design | `get_type_info` searches the entire arena linearly from front to back (`O(N)`) to locate the matching type pointer. | `arena_allocator.hpp:499` | **Performance Trap**: Extremely slow type lookup for large arenas, even though the header resides right before the object pointer. |
